package com.synapse.riskops.repository;

/**
 * IncidentRepository.java
 * Owner: Person 2 | Week: 5
 *
 * Spring Data JPA repository for IncidentEntity — persistence layer for the
 * incidents table. Extend JpaRepository<IncidentEntity, UUID> and add query
 * methods needed by IncidentController (e.g. findByStatus, findByServiceId).
 */
public interface IncidentRepository {
    // TODO Week 5: extend JpaRepository<IncidentEntity, UUID>
}
