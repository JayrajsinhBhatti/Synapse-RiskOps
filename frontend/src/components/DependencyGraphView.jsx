/*
  frontend/src/components/DependencyGraphView.jsx
  Owner: Person 2 | Week: 6

  Visualizes the service dependency graph (ml-engine/app/services/graph_builder.py,
  exposed via api/graph_traversal.py, proxied through the backend). Uses reactflow
  (already in package.json). Should highlight the propagation path when an
  incident's root cause is being traced.
*/
