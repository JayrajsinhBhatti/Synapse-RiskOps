/*
  frontend/src/components/RiskScorePanel.jsx
  Owner: Person 2 | Week: 6

  Displays live/recent risk scores per service (from ml-engine's RiskScorer,
  via backend). Should visually flag scores crossing the tiered thresholds
  (healthy / watch / incident-triggering) defined in Week 2. Uses recharts
  (already in package.json).
*/
