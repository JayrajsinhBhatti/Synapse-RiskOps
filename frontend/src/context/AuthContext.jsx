/*
  frontend/src/context/AuthContext.jsx
  Owner: Person 2 | Week: 8 (aligned with backend auth phase)

  React context providing auth state (JWT token, current user) across the app.
  Wraps App.jsx once backend JWT auth (JwtAuthFilter.java) is implemented.
*/
