/*
  frontend/src/api/client.js
  Owner: Person 2 | Week: 6

  Axios instance configured with the backend base URL (VITE_API_BASE_URL from
  .env, proxied to Spring Boot in vite.config.js during dev). Add request/response
  interceptors here (e.g. attach JWT once auth lands in backend Week 8).
*/
